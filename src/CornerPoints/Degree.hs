module CornerPoints.Degree (Degree(..)) where

{- |Anywhere that a degree of a circle is required, use this to make type signatures more readable.
Having it here in a single place, saves declaring it in multiple files where more readable type signatures are requred..-}
type Degree = Double
